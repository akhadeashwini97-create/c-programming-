#include <stdio.h>

int main() {
    float base = 5.0;
    float height = 4.0;
    float area;

    area = 0.5 * base * height;

    printf("Area of triangle = %.2f\n", area);
    return 0;
}
