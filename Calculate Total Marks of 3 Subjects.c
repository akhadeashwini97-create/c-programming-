#include <stdio.h>

int main() {
    int maths = 80;
    int science = 70;
    int english = 75;
    int total = maths + science + english;

    printf("Total Marks = %d\n", total);
    return 0;
}
