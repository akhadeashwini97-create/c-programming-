#include <stdio.h>

int main() {
    const float PI = 3.14;
    float radius = 5.0;
    float area = PI * radius * radius;
    printf("The area of the circle is %.2f", area);
    return 0;
}
