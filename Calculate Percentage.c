#include <stdio.h>

int main() {
    int totalMarks = 270;
    int maxMarks = 300;
    float percentage = (totalMarks * 100.0) / maxMarks;

    printf("Percentage = %.2f%%\n", percentage);
    return 0;
}
